import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://eurocall-faragiannis.gr',
  // Static output — δωρεάν Netlify, άριστο SEO, μηδέν συντήρηση.
  output: 'static',
});
