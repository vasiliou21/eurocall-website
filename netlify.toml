# Eurocall — Netlify static Astro deploy
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/proionta/*"
  to = "/proionta/c"
  status = 200
